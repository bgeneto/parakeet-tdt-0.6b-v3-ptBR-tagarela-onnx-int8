# Build context is requirements.txt + download_model.py + app.py (see Dockerfile COPY).
# Keep the upload small and avoid checksum noise from local artifacts.
.git
.gitignore
.gitattributes
.env
.env.*
.venv
**/__pycache__
**/*.pyc
**/.venv
models
*.onnx
*.onnx_data
client_example.py
compose.yaml
*.md
*.example
